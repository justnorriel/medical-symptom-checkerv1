version: '3.8'

services:
  medical-symptom-checker:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
    container_name: medical-symptom-checker
